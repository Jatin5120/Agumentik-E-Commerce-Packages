enum MenuState {
  home,
  favourite,
  message,
  profile,
}

enum DialogType {
  normal,
  success,
  warning,
  error,
}

enum ButtonType {
  primary,
  secondary,
  outlined,
}

enum ButtonSize {
  small,
  medium,
  large,
}

enum DiscountType {
  fixed,
  percent,
  none,
}

enum Gender {
  male,
  female,
  others,
  notSpecified,
}
